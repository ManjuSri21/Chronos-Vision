Put your background song here as:

  bg.mp3

Example:
  Chronos-Vision/public/bg.mp3

Then rebuild:
  npm run build

Notes:
- Browsers block autoplay with sound. In this app, audio starts after you click the sound button.
- If the file is missing, the app will fall back to an online demo track.
